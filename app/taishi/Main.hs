main :: IO ()
main = do
  
  -- initBoard boardSize [((center,center),O), ((center,center+1),X),((center+1,center),X), ((center+1,center+1),O)]
  -- roop boardInfo action canPut checkWin checkDraw checkLose O
  --   where
  --     center = boardSize `div` 2
